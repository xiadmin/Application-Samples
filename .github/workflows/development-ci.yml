name: Development CI

on:
  push:
    branches: [development]

permissions:
  contents: read

jobs:
  build-and-test:
    uses: xiadmin/Application-Samples-Infra/.github/workflows/development-ci.yml@main
    with:
      source_repository: ${{ github.repository }}
      source_ref: ${{ github.sha }}
