cmake_minimum_required(VERSION 3.16)

project({{binary_name}} VERSION 0.1.0 LANGUAGES {{project_lang}})

include("${CMAKE_CURRENT_LIST_DIR}/{{cmake_include_path}}/CMakeLists.txt")
ximea_find_sdk(REQUIRED)

add_executable({{target}} {{source_file}})
target_compile_features({{target}} PRIVATE {{lang_std}})
target_link_libraries({{target}} PRIVATE {{ximea_target}})

sample_flat_output_directories(TARGET {{target}})

install(TARGETS {{target}} RUNTIME DESTINATION bin)
